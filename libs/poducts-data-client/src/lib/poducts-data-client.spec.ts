import { poductsDataClient } from './poducts-data-client';

describe('poductsDataClient', () => {
  it('should work', () => {
    expect(poductsDataClient()).toEqual('poducts-data-client');
  });
});
